import SwiftUI

// Compatibility alias for the existing match route. Phase 3 supersedes the earlier
// visual/control implementations while preserving the stable match call site.
typealias PingoEightBallPhase1View = PingoEightBallPhase3View
