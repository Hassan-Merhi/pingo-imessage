# Pingo backend boundary

A live backend is intentionally not deployed in Wave 1.

Future server responsibilities are profiles/usernames, durable aggregate statistics, achievement state, cosmetic inventory, premium-pack entitlements, and abuse controls. Game turns remain iMessage-first.

This directory becomes the backend service root when the identity/server architecture is implemented in the profiles/multiplayer waves.
